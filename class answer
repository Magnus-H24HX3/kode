class Answer {
  int x;
  int y;
  int w;
  int h;

  Answer(int x, int y, int w, int h) {
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
  }

  boolean isPointInside(int px, int py) {
    return (px > x && px < x+w) && (py > y && py < y+h);
  }
}
